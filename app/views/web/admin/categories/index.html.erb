<% provide(:title, 'Admin category') %>
<div class="thema-text">Администрирование категорий</div>

<div class="button_new_record_db">
  <%= link_to 'Новая категория...', new_admin_category_path, :class => 'btn btn-primary' %>
</div>
<table class="table table-bordered">
	<thead>
		<tr>
			<th><%= sort_link @q, :id %></th>
      <th><%= sort_link @q, :created_at %></th>
			<th><%= sort_link @q, :name %></th>
      <th><%= sort_link @q, :section %></th>
			<th></th>
		</tr>

		<tbody>
      <% @categories.each do |item| %>
        <tr>
        	<td><%= item.id %></td>
          <td><%= item.created_at %></td>
          <td><%= item.name %></td>
          <td><%= item.section.name %></td>
        	<td>
            <div class="btn-group">
              <%= link_to edit_admin_category_path(item), class: 'btn btn-success btn-sm' do %>
                Изм.
              <% end %>

              <%= link_to admin_category_path(item), method: :delete, data: { confirm: "Вы уверены?" }, class: 'btn btn-danger btn-sm' do %>
                Удал.
              <% end %>
            </div>
        	</td>
        </tr>
      <% end %>
    </tbody>
	</thead>
</table>

<%= will_paginate @categories %>